<!DOCTYPE html>
<html lang="en">

  <head>

    <meta charset="UTF-8" />
    <title>Admin Edit Movie Page</title>
    <style>

      body {
        font-family: Arial, sans-serif;
        color: #0f172a;
      }

      .container {
        max-width: 500px;
        margin: 40px auto;
        padding: 0 20px;
      }

      .msg {
        color: #b91c1c;
        margin-bottom: 12px;
      }

      .btn {
        background-color: #14213d;
        color: #ffffff;
        border: none;
        cursor: pointer;
        padding: 8px 16px;
        text-decoration: none;
        display: inline-block;
      }

      .btn:hover {
        background-color: #1d4ed8;
      }

    </style>

  </head>

  <body>

    <div class="container">

      <h1>Admin Edit Movie Page</h1>

      <% if (msg) { %>
        <div class="msg"><%= msg %></div>
      <% } %>

      <form action="/admin/movies/<%= movie._id %>/edit" method="POST" enctype="multipart/form-data">

        Title<br>
        <input type="text" name="title" value="<%= movie.title %>" /><br><br>

        Genre<br>
        <input type="text" name="genre" value="<%= movie.genre %>" /><br><br>

        Director<br>
        <input type="text" name="director" value="<%= movie.director || '' %>" /><br><br>

        Release Year<br>
        <input type="number" name="releaseYear" value="<%= movie.releaseYear || '' %>" /><br><br>

        Description<br>
        <textarea name="description" rows="4"><%= movie.description || '' %></textarea><br><br>

        Movie Image<br>
        <input type="file" name="movieImage" accept="image/*"><br>
        <small>Leave empty to keep existing image</small><br><br>
        <button type="submit" class="btn">Save Changes</button>

      </form>

      <br>
      <a href="/admin/movies" class="btn">Back to movie list</a>

    </div>

  </body>

</html>