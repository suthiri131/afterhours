<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Review</title>
  <link rel="stylesheet" href="/css/review-form.css" />
  <link rel="stylesheet" href="/css/review-movie-poster.css" />
</head>
<body>
  <%- include("../navbar") %>
  <div class="page-container">
    <a href="/movies/<%= movie._id %>" class="back-link">← Back to Movie Details</a>

    <% if (error) { %>
      <div class="error-box"><%= error %></div>
    <% } %>

    <div class="review-layout">
      <section class="panel">
        <h2 class="section-label">Step 1: Edit Tips</h2>
        <%- include("review-movie-poster", {
          movie: movie,
          showAverageBox: false
        }) %>

        <hr>
        <ul class="guideline-list">
          <li>Update your headline if needed.</li>
          <li>You can change one or all category ratings.</li>
          <li>Keep your review clear and helpful.</li>
          <li>Make sure your updated review still matches your rating.</li>
        </ul>
      </section>

      <section class="panel">
        <h2 class="section-label">Step 2: Update Your Rating</h2>
        <p class="section-subtext">Update your rating below.</p>

        <form action="/reviews/<%= review._id %>/edit" method="POST">
          <%- include("review-rating-fields", { formData: formData }) %>

          <%- include("review-form-fields", {
            formData: formData,
            movie: movie,
            formTitle: "Step 3: Update Your Review",
            subtext: "Update your headline and review below.",
            reviewPlaceholder: "Update your thoughts here...",
            submitLabel: "Update Review"
          }) %>
        </form>
      </section>
    </div>
  </div>
</body>
</html>